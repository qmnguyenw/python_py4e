Heap queue (or heapq) in Python



Heap data structure is mainly used to represent a priority queue. In Python,
it is available using “ **heapq** ” module. The property of this data
structure in Python is that each time the **smallest of heap element is
popped(min heap)**. Whenever elements are pushed or popped, **heap structure
in maintained**. The heap[0] element also returns the smallest element each
time.

 **Let’s see various Operations on heap :**

 ****

* heapify(iterable)

